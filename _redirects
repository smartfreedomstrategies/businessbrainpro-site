# Cloudflare Pages redirects — businessbrainpro.com
# Ecosystem infrastructure paths. One rule per line: <from> <to> <status>.
#
# /template is the STABLE redirect the Command Center skills ship (command-center-setup,
# setup-doctor). Skills carry ONLY this stable path — never the raw Notion share link
# (it embeds a page id — RULE ZERO). Change the destination here, never in the plugins.
# Destination = Command Center — Template v1 (schema 1.0.0), recorded in the umbrella repo's
# versions.md. 302 (temporary) so the destination can change without clients re-caching.

/template         https://rose-spleen-e47.notion.site/Command-Center-Template-v1-aa145dba04ac8288af4481b047bf4f16   302
/template-notion  https://rose-spleen-e47.notion.site/Command-Center-Template-v1-aa145dba04ac8288af4481b047bf4f16   302
